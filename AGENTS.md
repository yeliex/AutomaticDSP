# AutomaticDSP Agent 指南

AutomaticDSP 是《戴森球计划》的自动化控制 Mod。项目目标是通过程序化接口读取游戏状态，并让外部 AI Agent 在不绕过游戏规则的前提下控制游戏。

## 项目目标

- 输出能力：提供 HTTP 接口查询游戏内状态，让外部程序能够感知当前游戏、玩家、背包、科技、当前行星、工厂实体、矿脉、生产、电力和提示等状态。
- 输入能力：提供顺序任务队列，允许外部程序提交移动、采集、背包制造、建造、设置配方、等待条件等游戏内指令。
- 第一阶段目标：完成行星内基础生产线建立能力，包括伊卡洛斯移动、采矿、背包制造、放置建筑、铺设传送带、放置分拣器、设置制造台/熔炉/研究站配方，并能按游戏内完成状态确认任务成功。
- 后续目标：扩展到行星内/星际物流、戴森球相关能力，但仍遵守本文件的边界约束。

## 核心边界

Mod 只提供“游戏内能力”的程序化入口，不提供越过游戏本身规则的策略能力。

必须遵守：

- 不绕过科技、物品、距离、碰撞、地形、电力、端口连接、建造范围、无人机建造等游戏规则。
- 不用 Mod 替外部 Agent 选择矿区、规划产线、优化布局、计算蓝图、决定科技路线或做资源调度策略。
- 不暴露“自动选址”“自动规划矿机”“自动生成产线”等策略接口。
- 不把面向规划的派生字段塞进游戏状态摘要，例如在 `factoryDetails` 中额外加入端口规划、矿机候选、推荐路径等。
- 如果需要坐标、方向、端口、插槽、碰撞或地形信息，优先通过与游戏对象对齐的通用查询读取原始字段，再由外部 Agent 计算。

## 状态查询原则

状态查询接口应该尽量和游戏对象结构对齐。

推荐：

- 查询 `factory` / `localFactory` 读取当前行星原始 `PlanetFactory` 对象中的 `entityPool`、`veinPool`、`vegePool` 等字段。
- 查询 `items`、`recipes`、`techs` 等原型摘要，用于理解物品、配方和科技状态。
- 查询 `factoryDetails` 只用于快速确认已建实体、组件状态、配方、产出堵塞、缺料等常用运行摘要。
- 如果通用查询读不到基础 Unity/游戏结构，例如 `Vector3`、`Quaternion`、`Pose`、`Pose[]`，可以补通用序列化能力。

不推荐：

- 在状态摘要里增加“推荐建造位置”“矿机覆盖方案”“传送带路径建议”等外部规划结果。
- 为某个业务场景单独增加派生查询根，除非它确实对应游戏内已有系统或对象。

## 任务队列原则

任务队列表达外部 Agent 已经决定好的操作，执行必须顺序、可预测。

- `POST /tasks` 提交有序命令数组。
- 命令按顺序执行，前一条未完成前不执行后一条。
- 建造类命令只有在游戏内实际建成实体后才算成功，不能只以创建预建为成功。
- 每条命令应返回明确状态、失败原因和游戏原生校验结果。
- 如果目标超出游戏允许的下发范围，应返回失败，让外部 Agent 先移动伊卡洛斯。
- 命令可以调用游戏原生方法和校验逻辑，但不能伪造成功或绕过限制。

## 规划归属

以下内容属于外部 Agent、测试脚本或未来 skill，不属于 Mod 接口能力：

- 选择哪个矿脉或矿簇。
- 根据矿脉数量、距离、附近空间、传送带长度选择采矿机位置。
- 根据建筑端口、经纬线、直线/直角策略计算传送带路径。
- 决定发电机、电塔、制造台、熔炉、研究站的布局。
- 决定科技推进路线、资源优先级、手搓或自动化的取舍。
- 设计跨星际物流网络和戴森球建造策略。

Mod 可以提供完成这些计算所需的原始状态和执行命令，但不应替外部 Agent 做策略判断。

## Agent 操作优先级

为了尽可能提高科研效率，外部 Agent 操作应以生产线与发电量优先，尽快完成基础生产线的建立。科技推进应尽快提升物流、发电相关能力；必要时可以通过完成升级研究来提高后续效率，但升级研究本身也会消耗时间和资源，需要结合当前目标进行取舍。应合理利用储物仓作为临时缓冲和材料来源，可以从储物仓中取出手搓所需材料，但不应把不相关物品混放进储物仓。

## 建造与移动

- 移动是独立能力。外部 Agent 应根据状态查询决定是否需要先移动伊卡洛斯。
- 在游戏允许的合理范围内，命令执行可以触发游戏内原生靠近或建造流程。
- 超出范围时应返回 `out_of_range`，而不是扩大命令能力。
- 放置建筑、传送带、分拣器时，最终结果必须以游戏原生建造校验为准。
- 传送带路径可以由外部 Agent 显式传入点列；Mod 不应自动优化路径。

## 开局和特殊对象

- 开局飞行仓、地面资源等应通过游戏状态查询感知。
- 若对象本身在游戏中是 `vegePool`、`entityPool`、`veinPool` 等对象，应尽量通过对应原始对象查询和普通命令处理。
- 不应为飞行仓这类特殊场景长期暴露不通用的专用策略命令。

## 实现偏好

- 优先复用游戏原生方法和校验。
- 优先保持接口小而明确。
- 优先返回游戏原生错误或可追溯到原生校验的错误。
- 新增字段或命令前，先判断它是“游戏能力”还是“外部策略”。只有前者适合进入 Mod。
- 文档中可以记录 Agent 的建议策略，但代码接口不要固化这些策略。
