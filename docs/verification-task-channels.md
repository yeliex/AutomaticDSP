# 任务通道验证

2026-09-25，在 `AutomaticDSP-redblue-opening-20260925` 实测新版调度。

## 行为

- 科研、手搓自动进入各自原生队列；等待完成时在后台跟踪。
- 机甲指令互斥；建造校验和预建下达占用机甲通道，等待落成时释放。
- 垃圾、队列移除、提示确认自动即时执行，无需 `immediate:true`。
- `dependsOn` 只允许同任务前序命令；实体 `commandId` 引用自动形成依赖。
- 制造完成依照本次原生任务的剩余次数，不依赖背包净增。
- 默认制造／科研只确认入队；设置等待参数为 true 才能通过完成依赖等待产物／解锁。

## 验证结果

编译 0 警告、0 错误，Skill 校验通过。链接真实调度源码的隔离回归检查 13 项通过，覆盖通道分流、预建释放、任务完成状态、引用依赖、跨任务取消／超时隔离、显式依赖、机甲互斥、无效依赖及失败停止。

游戏内 `task:1` 没有指定 immediate：20 秒指令等待仍在运行时，科研 1112 与丢弃石矿在同一帧完成下达；等待完成模式的 1 个铁块制造独立完成。另一任务的垃圾拾回也无需等待指令通道，石矿恢复 46 个。

`task:3` 铺设油管末段 19 个带段：

| 命令 | 开始（UTC） | 完成（UTC） |
| --- | --- | --- |
| oil-belt | 03:54:56.013 | 03:55:19.039 |
| follow 移动 | 03:54:56.013 | 03:54:57.585 |
| built-barrier 依赖等待 | 03:55:19.039 | 03:55:19.039 |

移动与施工重叠，完成依赖在实际落成后放行，最后无遗留预建。轮询未抓到短暂的双 RUNNING 画面，以上结论由命令起止时间确认。通过 `/game/save` 覆盖保存。

测试源码与响应：`.tools/queue-validation/`、`.tools/opening-validation/queue-live-lanes.json`、`queue-live-construction.json`。隔离测试不模拟游戏原生物理与资源规则，真实建造验证补充了这一部分。
