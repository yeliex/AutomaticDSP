# 垃圾操作与接口退出验证

2026-09-25，游戏版本 0.10.35.29088，存档 `AutomaticDSP-redblue-opening-20260925`。

- 编译成功，0 警告、0 错误；Skill 校验通过。
- 丢弃 1 个石矿：背包 46 → 45，状态返回新垃圾、物品数量与拾取范围。
- 对同一垃圾下达拾取，原生吸取后背包 45 → 46，垃圾数量恢复基线。
- 错误物品 ID 返回 `target_changed`；数量 0、`clearTrash scope:local` 返回 `invalid_command`。
- `/game/save` 对同一存档名连续保存两次均成功，支持覆盖。
- `/game/exit` 只接受退出；调用前后该存档修改时间不变。保存与退出为独立操作。

原生实现核对：垃圾由 `TrashSystem.container` 两个同索引池描述，落地星球和邻近星球不能混用。拾取使用原生高度相关距离公式，并交由原生吸取倒计时处理入包和溢出。全局清理调用原生 `ClearAllTrash`，不额外提供局部清理行为。

本次没有破坏性执行全局清理，也没有在其他星球或背包满的情况下实测；这些分支依据原生代码保留，不能视为已完成现场验证。垃圾槽位会回收，使用前应刷新状态；物品 ID 校验不能替代完整对象代际标识。

本地验证脚本与详细响应位于 `.tools/opening-validation/verify-trash.mjs`、`trash-verification.json`，不包含反编译源码。
